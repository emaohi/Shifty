machine:
  environment:
    DJANGO_SETTINGS_MODULE: settings.circle

test:
  pre:
    - python manage.py migrate
    - pylint --disable=R,C,no-member,no-init core log

deployment:
  staging:
    branch: master
    heroku:
      appname: shifty-app